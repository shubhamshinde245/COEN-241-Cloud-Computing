version: '3'
services:
  bookclub:
     build:
         context: bookclub
     ports:
         - "5000"

  nginx:
      image: nginx:latest
      volumes:
              - ./nginx.conf:/etc/nginx/nginx.conf:ro
      depends_on:
          - bookclub
      ports:
          - "80:80"